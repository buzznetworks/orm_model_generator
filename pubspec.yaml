name: orm_model_generator
description: A ORM model generator (currentlly supports only Aqueduct and PostgreSQL
version: 0.0.1

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  code_builder: ^3.3.0

  dart_style: ^1.3.6

  inflection2: ^0.4.3

  postgres: ^2.1.1

  meta: ^1.1.8

  recase: ^3.0.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
